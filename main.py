# This is main python file
from add import add #importing the add function from the file add.py
from sub import sub
from multi import multi

#main part of the code
print(add(2, 2)) #printing the sum of the numbers
print(sub(2, 2)) #printing the sub of the numbers
print(multi(2, 2)) #printing the multi of the numbers

# GUI Functions added
